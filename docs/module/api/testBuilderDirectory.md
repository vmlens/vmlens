# Problem

A directory to store all additional pre analyzed classes

# Solution

One configurable directory
The name of the .vmlens file contains the name of the project the plugin
is running in and or a configured name
