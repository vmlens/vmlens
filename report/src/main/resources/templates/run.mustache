{{>header.mustache}}

<!-- run.mustache -->

<div class="container-fluid">
    <div class="row">
        <table class="table table-striped text-start">
            <tr>
                <th>Thread</th>
                <th>Operation</th>
                <th>Method</th>
            </tr>
            {{#elements}}
                <tr>
                    {{#isSeparator}}
                        <td colspan="3">{{{operation}}}</td>
                    {{/isSeparator}}
                    {{^isSeparator}}
                        <td>{{threadName}}</td>
                        <td>{{{operation}}}</td>
                        <td>{{#hasLink}}<a href="{{link}}">{{method}} </a> {{/hasLink}}{{^hasLink}}  {{method}} {{/hasLink}}  </td>
                    {{/isSeparator}}
                </tr>
            {{/elements}}
        </table>
    </div>
</div>
{{>footer.mustache}}