package com.vmlens.trace.agent.bootstrap.callback.state;

public class ObjectStateMultiThreaded extends ObjectStateAbstractMultiThreaded {

	
	/*
	 * boolean sendEvent = true;
				if (MemoryAccessType.containsWrite(operation)) {
					if (state.writeEventCount > 200) {
						sendEvent = false;
					} else {
						state.writeEventCount++;
					}

				} else {
					if (state.readEventCount > 200) {
						sendEvent = false;
					} else {
						state.readEventCount++;
					}
				}
				
	*/
	
	
	



}
